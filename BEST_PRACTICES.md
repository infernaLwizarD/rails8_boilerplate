# Стандартные практики Rails Engine

## 🎯 Типы Rails Engine

### 1. Библиотечный Engine (Library Engine)
**Примеры:** Devise, Pundit, Kaminari, SimpleForm

**Характеристики:**
- ✅ Код остаётся в engine
- ✅ Предоставляет готовую функциональность
- ✅ Минимальная кастомизация через конфигурацию
- ✅ Обновляется через `bundle update`

**Структура:**
```
devise/
├── app/
│   ├── controllers/    # Контроллеры в engine
│   ├── models/         # Модели в engine
│   └── views/          # Views в engine
└── lib/
    └── generators/     # Генераторы для копирования initializers
```

**Использование:**
```ruby
# Только initializer копируется в приложение
rails generate devise:install
rails generate devise User

# Остальной код остаётся в engine
```

---

### 2. Boilerplate Engine (Starter Template)
**Примеры:** Rails8Boilerplate, Rails Composer, Suspenders

**Характеристики:**
- ✅ Генератор копирует весь код в приложение
- ✅ Полный контроль над кодом
- ✅ Легко модифицировать под проект
- ✅ Engine используется только для UI компонентов

**Структура:**
```
rails8_boilerplate/
├── app/
│   ├── assets/         # Остаётся в engine (AdminLTE)
│   ├── helpers/        # Остаётся в engine
│   └── views/
│       └── layouts/    # Остаётся в engine (шаблоны)
├── lib/
│   └── generators/
│       └── templates/  # Шаблоны для копирования
│           ├── models/
│           ├── controllers/
│           ├── views/
│           └── specs/
```

**Использование:**
```ruby
# Генератор копирует ВСЁ в приложение
rails generate rails8_boilerplate:install

# Результат: весь код в вашем приложении
my_app/
├── app/
│   ├── models/user.rb           # Скопировано
│   ├── controllers/web/         # Скопировано
│   ├── views/web/               # Скопировано
│   └── policies/                # Скопировано
```

---

## 📋 Что копировать, а что оставлять в Engine

### ✅ Копировать в приложение (Boilerplate подход)

#### 1. **Модели** (`app/models/`)
```ruby
# Почему: Каждое приложение уникально, модели будут меняться
app/models/
├── user.rb
└── concerns/
    ├── user_repository.rb
    ├── user_ransack.rb
    └── user_role_enum.rb
```

#### 2. **Контроллеры** (`app/controllers/`)
```ruby
# Почему: Бизнес-логика специфична для каждого приложения
app/controllers/web/
├── application_controller.rb
├── home_controller.rb
├── users_controller.rb
└── users/
    ├── sessions_controller.rb
    ├── registrations_controller.rb
    └── passwords_controller.rb
```

#### 3. **Policies** (`app/policies/`)
```ruby
# Почему: Правила авторизации уникальны для каждого приложения
app/policies/
├── application_policy.rb
└── user_policy.rb
```

#### 4. **Views** (кроме layouts)
```ruby
# Почему: UI специфичен для каждого приложения
app/views/web/
├── home/
│   └── index.html.erb
└── users/
    ├── index.html.erb
    ├── show.html.erb
    ├── new.html.erb
    └── edit.html.erb
```

#### 5. **Initializers**
```ruby
# Почему: Настройки специфичны для каждого приложения
config/initializers/
├── devise.rb    # Копировать (mailer_sender, secret_key и т.д.)
└── pagy.rb      # Копировать (настройки пагинации)
```

#### 6. **Миграции**
```ruby
# Почему: Схема БД специфична для каждого приложения
db/migrate/
└── 20250130_devise_create_users.rb  # Копировать с новым timestamp
```

#### 7. **Тесты**
```ruby
# Почему: Тесты должны быть в приложении для запуска
spec/
├── models/
│   └── user_spec.rb
├── controllers/
│   └── web/
│       └── users_controller_spec.rb
├── policies/
│   └── user_policy_spec.rb
└── features/
    └── users/
        ├── create_spec.rb
        ├── update_spec.rb
        └── delete_spec.rb
```

#### 8. **Локализации** (опционально)
```ruby
# Почему: Переводы могут меняться
config/locales/
├── ru.yml
└── pagy.ru.yml
```

---

### 🎨 Оставлять в Engine

#### 1. **Assets** (`app/assets/`)
```ruby
# Почему: Стандартизированные стили/скрипты (AdminLTE)
app/assets/
├── stylesheets/
│   └── adminlte/
├── images/
│   └── adminlte/
└── javascripts/
    └── adminlte/
```

#### 2. **Layouts** (`app/views/layouts/`)
```ruby
# Почему: Стандартные шаблоны страниц
app/views/layouts/
└── lte/
    ├── application.html.erb
    ├── auth.html.erb
    └── partials/
        ├── _header.html.erb
        ├── _sidebar.html.erb
        └── _footer.html.erb
```

#### 3. **Helpers** (`app/helpers/`)
```ruby
# Почему: Универсальные вспомогательные методы
app/helpers/
├── pagy_helper.rb
├── application_helper.rb
└── form_helper.rb
```

#### 4. **Генераторы** (`lib/generators/`)
```ruby
# Почему: Инструменты для создания кода
lib/generators/
├── custom_resource/
│   ├── custom_resource_generator.rb
│   └── templates/
└── rails8_boilerplate/
    └── install_generator.rb
```

---

## 🚀 Примеры из реальных проектов

### Devise (Library Engine)
```ruby
# Генератор копирует ТОЛЬКО initializer и миграции
rails generate devise:install
# → config/initializers/devise.rb

rails generate devise User
# → db/migrate/devise_create_users.rb
# → app/models/user.rb (с минимальным кодом)

# Контроллеры, views остаются в engine
# Кастомизация через наследование:
class Users::SessionsController < Devise::SessionsController
  # Переопределяем методы
end
```

### Rails Composer (Boilerplate)
```bash
# Копирует ВСЁ в приложение
rails new myapp -m https://raw.github.com/RailsApps/rails-composer/master/composer.rb

# Результат: полноценное приложение с кодом
```

### Suspenders (Thoughtbot Boilerplate)
```bash
# Создаёт новое приложение с готовым кодом
suspenders myapp

# Результат: приложение с настроенными gems, тестами, CI/CD
```

---

## 📝 Checklist для Boilerplate Engine

### При создании генератора установки:

- [ ] **Копировать модели** - `directory 'app/models', 'app/models'`
- [ ] **Копировать контроллеры** - `directory 'app/controllers/web', 'app/controllers/web'`
- [ ] **Копировать policies** - `directory 'app/policies', 'app/policies'`
- [ ] **Копировать views** - `directory 'app/views/web', 'app/views/web'`
- [ ] **Копировать тесты** - `directory 'spec', 'spec'`
- [ ] **Копировать initializers** - `template 'templates/devise.rb', 'config/initializers/devise.rb'`
- [ ] **Копировать миграции** - `migration_template 'db/migrate/devise_create_users.rb'`
- [ ] **Копировать локализации** - `directory 'config/locales', 'config/locales'`
- [ ] **Добавить routes** - `inject_into_file 'config/routes.rb'`
- [ ] **Показать инструкции** - что скопировано, что осталось в engine

### Оставить в Engine:

- [ ] **Assets** - `app/assets/`
- [ ] **Layouts** - `app/views/layouts/`
- [ ] **Helpers** - `app/helpers/`
- [ ] **Генераторы** - `lib/generators/`

---

## 💡 Преимущества Boilerplate подхода

### Для разработчика:
1. ✅ **Полный контроль** - весь код в приложении
2. ✅ **Легко модифицировать** - не нужно переопределять через monkey patching
3. ✅ **Понятно** - видно весь код, нет "магии"
4. ✅ **Независимость** - можно удалить engine после установки

### Для проекта:
1. ✅ **Быстрый старт** - готовая структура за минуты
2. ✅ **Стандартизация** - единый подход во всех проектах
3. ✅ **Гибкость** - легко адаптировать под нужды
4. ✅ **Обучение** - новые разработчики видят готовые примеры

---

## 🎓 Рекомендации

### Когда использовать Library Engine:
- Универсальная функциональность (аутентификация, пагинация)
- Нужны обновления через `bundle update`
- Минимальная кастомизация
- Примеры: Devise, Pundit, Kaminari

### Когда использовать Boilerplate Engine:
- Стартовый шаблон для новых проектов
- Нужен полный контроль над кодом
- Специфичная бизнес-логика
- Примеры: Rails8Boilerplate, Rails Composer

### Гибридный подход (Rails8Boilerplate):
- **Копировать:** модели, контроллеры, views, тесты
- **Оставить в Engine:** UI компоненты (AdminLTE), layouts, helpers
- **Результат:** баланс между контролем и стандартизацией

---

## 📚 Дополнительные ресурсы

- [Rails Engines Guide](https://guides.rubyonrails.org/engines.html)
- [Devise Wiki](https://github.com/heartcombo/devise/wiki)
- [Rails Composer](https://github.com/RailsApps/rails-composer)
- [Suspenders](https://github.com/thoughtbot/suspenders)
